name: "hello-world-workflow"

on:
  push:
    branches:
      - main
      - production

jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello world
        run: echo "Hello, World!"